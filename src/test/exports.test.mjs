import * as models from '../core/models';
import * as routes from '../core/routes';

export const {
  createModels,
  getModels,
} = models;
export const {
  createRoute,
  getRoutes,
  getKoaRoutes,
  getSocketRoutes,
} = routes;
